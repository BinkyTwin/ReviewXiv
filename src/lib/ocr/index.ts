/**
 * OCR Types Module
 *
 * Type definitions for the Smart PDF Viewer
 * Text extraction is now handled by Docling (see @/lib/docling)
 */

// Types
export type {
  TextBlock,
  PageLayout,
  OCRStatus,
  TextBlockHighlight,
  TextSelection,
} from "./types";
